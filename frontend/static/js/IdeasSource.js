//fetch(url).then(res => res.json()).then(fileContent => fileContent)
//const snapshot = Object.entries(fileContent.Manga4Up).reverse().splice(0, 10);

/*
fetch(url).then(res => res.json())
  .then(fileContent => {
    //const snapshot = error ? error : fileContent;
    const listLimit = Object.entries(fileContent['List']).reverse();
    const recentLimit = Object.entries(fileContent['Manga4Up']).reverse().splice(0, 20);
    
    recentManga = recentLimit;
    mangaList = listLimit;
  })
*/
